# Saffron & Rice. PLACEHOLDER_ORIGIN is replaced at deploy with the
# site host plus any base path (one sed pass, see deploy/ORIGIN-SWAP.md;
# same token as sitemap.xml and every page head).
User-agent: *
Allow: /

Sitemap: https://PLACEHOLDER_ORIGIN/sitemap.xml
